---
import type { CatalogItem } from '#lib/catalog/catalog-types.ts';

import Anchor from '#components/parts/anchor.astro';
import LabeledSection from '#components/parts/labeled-section.astro';
import { t } from '#lib/i18n/i18n-strings.ts';

interface Props {
	entries: Array<CatalogItem> | undefined;
}

const { entries } = Astro.props;
---

{
	entries && entries.length > 0 ? (
		<LabeledSection data-nosnippet data-pagefind-ignore title={t('section.relatedWriting')}>
			<ul class="flex flex-col gap-1 text-sm">
				{entries.map(({ title, url }) => (
					<li>
						<Anchor anchorColor="accent" class="anchor-underline" href={url}>
							{title}
						</Anchor>
					</li>
				))}
			</ul>
		</LabeledSection>
	) : undefined
}
