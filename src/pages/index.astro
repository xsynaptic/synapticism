---
import * as R from 'remeda';

import IndexLayout from '#layouts/index.astro';
import { queryPostsIndex } from '#lib/collections/posts/posts-utils.ts';
import { queryProjectsIndex } from '#lib/collections/projects/projects-utils.ts';

const recentPosts = R.pipe(await queryPostsIndex(), R.take(5));
const selectedProjects = R.pipe(await queryProjectsIndex(), R.take(4));
---

<IndexLayout recentPosts={recentPosts} selectedProjects={selectedProjects} />
